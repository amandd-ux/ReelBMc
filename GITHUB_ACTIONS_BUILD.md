# Build APK with GitHub Actions

1. Upload this project to a GitHub repository.
2. Open the Actions tab.
3. Run "Build APK".
4. After completion, download the artifact named `app-debug-apk`.

No Android Studio is required.
